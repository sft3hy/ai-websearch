apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-websearch.fullname" . }}
  labels:
    {{- include "ai-websearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.portWeb }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-websearch.selectorLabels" . | nindent 4 }}
